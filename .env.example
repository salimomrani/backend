# Spring Profile (dev or prod)
SPRING_PROFILES_ACTIVE=dev

# Production Database Configuration (AWS RDS)
# Only needed when SPRING_PROFILES_ACTIVE=prod
DB_URL=jdbc:postgresql://your-rds-endpoint.region.rds.amazonaws.com:5432/backenddb
DB_USERNAME=your_username
DB_PASSWORD=your_secure_password

# Development Database Configuration (Docker)
# These are already configured in application-dev.yml
# POSTGRES_DB=backenddb
# POSTGRES_USER=backend_user
# POSTGRES_PASSWORD=backend_password
